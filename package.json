{
  "name": "hackathon-cobalt-spider",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@atlas/sdk": "^0.1.1",
    "cobalt-react-components": "https://github.com/Talkdesk/cobalt-react-components.git#v27.0.0",
    "github-api": "^3.2.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router-dom": "^5.0.1",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start:atlas": "atlas launch --config='./configuration.dev.json' --app-manifest='./manifest.json'",
    "start:app": "BROWSER=none react-scripts start",
    "start": "concurrently -k npm:start:*",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@atlas/cli": "^0.1.0",
    "concurrently": "^4.1.1"
  }
}
